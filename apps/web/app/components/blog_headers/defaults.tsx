import { SectionOption } from "@/app/types/section";

export const blogHeaderDeafult: SectionOption = {
  type: "site_component",
  key: "blogHeader",
  label: "Blog Header",
  classes: { width_all: "w-full" },
};
