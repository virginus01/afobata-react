import { SectionOption } from "@/app/types/section";

export const utility1Default: SectionOption = {
  type: "site_component",
  key: "utilityBill",
  label: "Utility Bill Services",
  classes: { width_all: "w-auto", padding_all: "p-1" },
};
